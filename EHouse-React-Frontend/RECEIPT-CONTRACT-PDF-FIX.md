# Receipt & Contract PDF Fix

Receipt and Contract reports now have a single **Download Receipt PDF** / **Download Contract PDF** button.

The previous HTML download behavior has been removed. The report is generated as a real PDF in the browser using html2pdf.js and saved with a `.pdf` extension.

No booking, payment, authentication, refresh, or database behavior was changed.
